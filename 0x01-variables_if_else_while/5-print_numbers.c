#include <stdio.h>
#include <stdlib.h>
/**
 * main - entry point
 *
 * return: always return 0
 *
 */
int main(void)
{
	int a;

	for (a = 0 ; a < 10 ; a++)
		printf("%d", a);
	printf("\n");
	return (0);
}
